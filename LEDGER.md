# LEDGER — Hotell shoppable demo (parents 142, 242, 264, 276, 515, 547, 902, 973)
Goal: spec §12 (16 metrics) + browser-shopable.
- [x] Design/API/cache fixes landed + live (rooms load, VERIFIED)
- [x] Calendar rebuilt as room×date matrix (Nicke 276.1) + cross-engine explicit-grid fix (276.2/276.3) — INTEGRATED, MASTER PUSHED = 0af6206 (VERIFIED)
- [x] Calendar DEPLOY to hosting main + ?v=0af6206 cache-bust — 276.4 → LIVE /hotell/ shows rebuilt booking matrix.
- [x] Room-IMAGE fix (PARENT 515): all 4 rooms show 4 DISTINCT real photos. Commit d0f57e9 pushed master. CODE VERIFIED via vision.
- [x] Deploy-REGRESSION & fix (PARENT 547): /hotell subpath-prefix preserved; deployed ad17229, cache-bust. LIVE RE-VERIFIED (4 rooms render, 4 distinct photos).
- [x] Booking-flow fix (PARENT 902): missing GET /api/rooms/{id} endpoint — booking modal showed "Boka Rum undefined". Added endpoint (MC 902.1, verified 902.2). Pushed master 21d7df1, mirrored to hosting (live endpoint 200).
- [x] Trailing-slash prefix-loss (PARENT 912, child 912.2, VERIFIED live 2026-09-03): GET /{room_id} defined without trailing slash caused FastAPI 307 redirect that dropped the /hotell mount prefix under `/api/rooms/{id}/` (the exact call booking-flow.js:427 makes) → 404 → "undefined". Fixed decorator to `@router.get("/{room_id}/")`, test both slash forms (8 passed). Pushed Hotell master c62831f, synced to hosting main ca6cae5, vm106 pull-timer rendered it. LIVE VERIFIED: curl /hotell/api/rooms/1/ → 200 direct; browser "Boka nu" → modal shows "Boka Rum 201 / Enkel 800 kr/natt" (not "undefined"). PROJETET FÄRDIGT.
- [x] Success-view room-number (PARENT 973, 2026-09-05): booking confirmation showed "Rum —" because closeModal() nulls currentRoom before showSuccess reads currentRoom.room_number. Fix captures roomNumber before closeModal, showSuccess(booking, roomNumber). Pushed Hotell master d9fd735, deployed to hosting main f98eaff, pushed d7f5a08 (+ ?v=f98eaff cache-bust on 25 static assets — mirror sync dc725d2 had stripped the ?v= refs). vm106 pull-timer rendered it. LIVE VERIFIED (browser, cache-busted URL): booking success view shows "Rum: Rum 101" (was "Rum —").
