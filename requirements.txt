# MC#2317 pin-fix for svarkor-ai/hotell -- exact pins only (vm106 build gate rejects >= ranges and the
# uvicorn[standard] extra). CHANGES: all >= -> == ; uvicorn[standard]>=0.34.0 -> uvicorn==0.34.0.
fastapi==0.115.0
uvicorn==0.34.0
sqlalchemy==2.0.36
pydantic-settings==2.7.0
aiosqlite==0.20.0
